import { walkResponeType } from './response';
interface propType {
    onFinish?: Function;
    isOpen?: boolean;
    onClose?: Function;
    color?: string;
    backgroundColor?: string;
    btnColor?: string;
    btnBackGroundColor?: string;
    actions?: Record<number, Function>;
    autoScroll?: boolean;
    image?: boolean;
    disableScrollSelector?: string;
    animation?: boolean;
    tooltip?: boolean;
    iconColor?: string;
    offlineFormData: walkResponeType;
}
declare const TourOfflineComponent: ({ onFinish, isOpen, onClose, backgroundColor, btnBackGroundColor, btnColor, color, actions, offlineFormData, autoScroll, image, disableScrollSelector, animation, tooltip, iconColor }: propType) => import("react/jsx-runtime").JSX.Element;
export default TourOfflineComponent;
