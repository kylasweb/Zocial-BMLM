import { common, discordTypes } from "../../types";
declare class Discord {
    private apiKey;
    private apiSecret;
    private config;
    private baseUrl;
    constructor(config: common.constructorConfig);
    connect({ entityId, userId, token, discordRedirectCode, redirectUri, }: discordTypes.connectDiscord): Promise<any>;
    disconnect({ entityId, userId, token, }: discordTypes.disconnectDiscord): Promise<any>;
    fetchMyGuilds({ entityId, userId, token }: discordTypes.fetchMyGuilds): Promise<any>;
    private fetchMyGuildRoles;
    private fetchGuildSceduledEvents;
}
export default Discord;
