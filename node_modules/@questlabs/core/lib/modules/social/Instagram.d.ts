import { common, instagramTypes } from "../../types";
declare class Instagram {
    private apiKey;
    private apiSecret;
    private config;
    private baseUrl;
    constructor(config: common.constructorConfig);
    connect({ entityId, userId, token, instagramRedirectCode, }: instagramTypes.connectInstagram): Promise<any>;
    disconnect({ entityId, userId, token, }: instagramTypes.disconnectInstagram): Promise<any>;
}
export default Instagram;
